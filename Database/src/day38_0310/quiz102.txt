SQL 기능에 따른 분류
DDL:
DML:
DCL:


1. 2

2. 1

3. 3 - 데이터베이스 시스템
4. 1 - 의미가 더이상 분리되지 않는 최소의 데이터 단위를 레코드라한다.
5. 1.

집합연산자 = 여러개의 결과 집합을 결합하거나 비교하는데 사용하는 연산자.
union, union all, intersect, except 등이 있음. select 문에서만 사용 가능.
6. 1
13. 4

<SELECT 문 문법>
SELECT column1, column2, ...
FROM table_name
WHERE condition
GROUP BY column
HAVING condition
ORDER BY column [ASC|DESC]
LIMIT number;

14. 4

NOT BETWEEN a AND b : a와 b사이값이 아닌 것 찾기.


