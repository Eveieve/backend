배열
- 한번 생성하면 길이 변경 불가. (고정길이 데이터 구조). 길이를 지정해주지 않으면 사용 불가.
- 더 많은 저장공간이 필요하다면 옮겨담아야 함. 더 큰 길이의 배열을 새로 만들고 이전 배열 항목들 복사해야 함.

클래스
- 객체지향 프로그래밍: 정의하기!
- 객체/클래스 차이점?
- 클래스 선언 방법? 시그니처란?
- 객체 생성과 클래스 변수
- 클래스 구성 멤버
- 필드 선언과 사용
- 생성자 선언과 호출 (생성자 오버로딩)
- 메소드 선언과 호출
- 패키지
- 접근 제한자
- Setters and Getters: 퍼블릭 이어야 함.
------------------------------
- 필드: 클래스에 정의된 멤버 변수들 지칭.
- 인스턴스 변수/멤버: 객체 생성(new) 후 사용할 수 있다. (객체에 소속된 멤버들임, 객체 생성을 한 후에 쓸 수 있는 멤버들임)
- 정적 멤버: 객체 생성 하지 않아도 사용할 수 있다. static members. 인스턴스가 아니라 클래스에 고정된 멤버. static 이 붙지 않은 멤버는모두 인스턴스 멤버임.
누구나 필요할때 쓸 수 잇는 공공재 멤버.
static zone 에서 main 메서드를 호출한다.

- final 필드와 상수
- 싱글톤 패턴.
- 빌더 패턴
- MVC 패턴.

---
임계영역 . critical section.
